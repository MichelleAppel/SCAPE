general:
  run_name: 'grayscale_coco'
  out_dir: './out'
  save_path: "${general.out_dir}/${general.run_name}/"
  base_path: "config/exp7_coco/base.yaml"

dataset:
  # Choose one of the following processing methods:
  processing: 'grayscale'
  # processing: 'canny'
  # processing: 'DoG'
  # processing: 'random'

evaluate:
  model_ckpt: out/grayscale_coco/decoder_epoch04.pt       # path to the .pt you want