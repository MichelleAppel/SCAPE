general:
  run_name: 'random_sun'
  out_dir: './out'
  save_path: "${general.out_dir}/${general.run_name}/"
  base_path: "config/exp6_sun/base.yaml"

# ─── DATASET SELECTION ──────────────────────────────────────
dataset:
  processing: 'random'
